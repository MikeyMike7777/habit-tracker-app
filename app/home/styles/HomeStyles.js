import { StyleSheet } from 'react-native';

export const HomeStyles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: 'bold',
    },
    link: {
        marginTop: 20,
        fontSize: 16,
        color: '#007AFF',
    },
    navText: {
        color: '#fff',
        fontSize: 16,
    },
});